#include <stdio.h>

/**
 * main - entry point to the programme that count
 * to 20.
 * Return: 0 on Success
 */
int main(void)
{
	int i;

	for (i = 1; i <= 20; i++)
		printf("%d\n", i);
	return (0);
}
